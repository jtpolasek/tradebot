# Tradebot

Paper-trading copy-trader (ETH + Base). **`PLAN.md` is the single source of truth — read it in full before doing anything, and follow its phases in order.** Track the current phase in this file's "Status" section below as you complete work.

## Non-negotiable rules (full detail in PLAN.md §0.4)

- Simulated money only. Never build real execution, never handle private keys.
- Never modify anything under `C:\Users\Willie\Documents\GMGN` (the predecessor app — read-only reference; PLAN.md §8 lists what to port from it).
- `pnpm build && pnpm test` must be green before declaring any phase done; commit at every milestone.
- Secrets only via `.env` (already created, gitignored). Never commit or print key values. `.env.example` mirrors it with blank values.
- TypeScript strict, ESM, viem (never ethers/web3.js), zod at boundaries, bigint for raw token amounts, lowercase addresses.
- Tests never touch the real DB or live network — `TEST_DATABASE_URL` only (port 5434, name must end in `_test`).
- If a command fails twice with the same error, stop and report instead of retrying.
- Ask before adding any dependency not listed in PLAN.md §2.

## Status

- Phase 0: **complete** (2026-06-10). 20 tests passing, committed 2f760ed.
- Phase 1: **complete** (2026-06-10). Committed 0024d63.
- Phase 2: **complete** (2026-06-10). 79 tests passing, committed 2302607.
- Phase 3: **complete** (2026-06-10). 91 tests passing, committed d23ad0c.
- Phase 4: **complete** (2026-06-10). 149 tests passing, committed f71c06a.
- Phase 5: **complete** (2026-06-11). 193 non-Docker tests passing, committed 747a828.
- Phase 6: **complete** (2026-06-11). 195 tests passing, committed 8a8d50d.
- Code review round 2: **complete** (2026-06-12). All findings 4.1–4.10 fixed on `fix/code-review-2026-06-11`. `pnpm build && pnpm test` green.
- Hybrid GMGN plan (2026-06-12): 6-part feature plan agreed — full plan in memory file `hybrid-gmgn-plan.md`, terminology in `CONTEXT.md`, key decision in `docs/adr/0001-persist-candidates-outside-scoring.md`. **Part 1 (candidate persistence) complete**: capped reconnect backfill window, added engine staleness veto via block timestamp (skip reason `stale-signal`), added `decode_status`/`confidence`/`reason` to `trade_signals`, persist candidate decodes instead of dropping them, prevent the engine from auto-copying candidates, and make scorer count decoded signals only. Validation: `pnpm build` and `pnpm test` green on 2026-06-12.
- Hybrid GMGN plan Part 2: **complete** (2026-06-12). Added candidate review queue: `review_status` workflow on `trade_signals`, API endpoints to list/copy/dismiss candidates, runner job that executes copy requests through the paper engine at current decision time, and `/candidates` dashboard page. Validation: `pnpm build` and `pnpm test` green.
- Candidate review hardening: **complete** (2026-06-12). Added store tests for candidate listing/status transitions and paper-engine test proving manual candidate copy uses the normal fill path while leaving the persisted signal as `decode_status='candidate'`. Validation: `pnpm build` and `pnpm test` green.
- Wallet validation hotfix: **complete** (2026-06-12). Root cause was an active malformed wallet row (`address='vein'`) being accepted through settings and then handed to ingest. Deactivated row `4fe658ca-7a2c-4c98-ad84-5bff03402e2a`, added API/store/UI validation and a legacy active-row guard. Re-check found no active malformed wallet rows. Validation: `pnpm build` and `pnpm test` green.
- Token display links: **complete** (2026-06-12). Signals, fills, candidates, and open positions now hydrate token symbol/name from `tokens` where available and the web UI links contract addresses to Etherscan or BaseScan. Validation: `pnpm build` and `pnpm test` green.
- Alchemy Free-tier getLogs hotfix: **complete** (2026-06-12). ETH reconnect backfill now uses 10-block `eth_getLogs` chunks to satisfy Alchemy Free-tier limits; Base already used 10-block chunks. Validation: `pnpm --filter @tradebot/ingest test` and `pnpm build` green.
- Parts 1–2 + hotfixes merged to `main` locally (2026-06-15, ff from `feat/candidate-persistence`).
- Hybrid GMGN plan now lives in `PLAN.md` as **Phase 8** (committed 2026-06-15); statuses tracked there. **Phase 8 complete** (all six parts done 2026-06-15).
- Hybrid GMGN plan Part 6 (links/names): **complete** (2026-06-15) on `feat/wallet-tx-links`. Added `WalletLink` (address → explorer + GMGN.ai) on leaders/settings and `TxLink` (txHash → explorer) on feed/candidates, plus `explorerAddressUrl`/`explorerTxUrl`/`gmgnWalletUrl` helpers. Token links shipped earlier. Validation: `pnpm build` and `pnpm test` green.
- Hybrid GMGN plan Part 5 (reprocess tool): **complete** (2026-06-15) on `feat/reprocess-tool`. Added pure `summarizeReprocess` + types in `packages/decoder`, and `scripts/reprocess.ts` (`pnpm reprocess [path…]`, defaults to `./recordings`) that re-derives signals from recordings via the live decoder and diffs against persisted `trade_signals`. Read-only over trading state. Unit tests for the diff; CLI smoke-tested. Validation: `pnpm build` and `pnpm test` green.
- Hybrid GMGN plan Part 4 (portfolio analytics): **complete** (2026-06-15) on `feat/portfolio-analytics`. Added pure `derivePortfolioAnalytics` + `getPortfolioAnalytics` store query, `GET /analytics`, a ported MetricStrip, and a PnL-by-token table on `/portfolio`. Metrics: win rate, realized PnL, fee drag, avg hold, open exposure, skip rate, fees paid, copied count. Unit + DB-integration tests. Validation: `pnpm build` and `pnpm test` green.
- Hybrid GMGN plan Part 3 (wallet toggles): **complete** (2026-06-15) on `feat/wallet-toggles`. Added `auto_copy` column to `wallets` (default true, migration 0005); surfaced `active` as a "Watching"/"Stop watching" toggle with a greyed "Not watching" list (re-enablable); added a per-wallet **Auto-copy** toggle. Engine caches auto-copy-off wallet IDs (refreshed on the 60s settings timer) and vetoes their **buys** with skip reason `auto-copy-off` — sells still flow through to exit positions, and manual candidate copies bypass it via `reviewStatus='copying'`. New `PATCH /wallets/:id` accepts `{ active?, autoCopy? }`. Validation: `pnpm build` and `pnpm test` green.
- Price/buy-decision improvements (`docs/price-buy-decision-improvements.md`): reviewed and reprioritised (2026-06-15). Resolved the four Open Decisions, added item 7 (Chainlink staleness gate, pending) and a cache-eviction note, and flagged the item-5 deepest-pool metric inconsistency. **Item 6 (unify sell fill modeling): complete** — extracted shared `PaperEngine` helpers (`modeledSlippageBps`, `priceSellFill`, `applySellToState`) so buys, copied sells, and exit sells share one fee/slippage/0x/accounting model; `executeExitSell` now uses 0x when configured (hydrating real token decimals) and falls back to spot otherwise. Note: exit sells now penalise null-liquidity impact like copied sells (was 0). Added integration test "routes exit-rule sells through 0x". `pnpm build` and `pnpm test` green (63 paper-engine tests).
- Price/buy-decision **item 7 (Chainlink staleness gate): complete** (2026-06-15). `getChainlinkEthUsd` now reads `updatedAt` and rejects rounds older than `MAX_CHAINLINK_STALENESS_SEC` (default 3600, in core config + `.env.example`); a stale round returns null → DefiLlama fallback → existing fallback-price-source buy veto. Added pricing tests for fresh (accepted) and stale (rejected → fallback) rounds. `pnpm build` and `pnpm test` green (23 pricing tests).
- Price/buy-decision **item 5 (liquidity selection): complete** (2026-06-15). Replaced `findDeepestV3Pool` (per-quote-pair, ranked by in-range L) with `findBestMarket(chain, token)` that scans every quote asset × venue × fee tier and picks the deepest **USD** market (`quote-balance × quoteUsd × 2`); in-range L is now only a tiebreak. Resolves the selection-vs-reporting metric inconsistency and makes price + liquidity derive from the same pool (`getUsdPriceResult`/`getLiquidityUsdResult` both use `findBestMarket`; market cached per token 5 min). Heavier cache-miss discovery (one `balanceOf` per candidate). Test: "selects the deeper USD market across quote assets for both price and liquidity". `pnpm build` and `pnpm test` green (24 pricing tests).
- **Uniswap V4 pricing (ADR 0002, `docs/uniswap-v4-pricing-plan.md`): Parts 1–4 complete** (2026-06-16). Root cause of LOCAL skipping with `no-liquidity-data`: pricing only scanned Uni V3 + Aerodrome CL, never V4 (singleton PoolManager, no on-chain pool discovery by pair). Fix prices a V4 pool by the **poolId observed in the leader's swap**. P1: decoder surfaces the V4 Swap `id` (poolId). P2: `poolId` added to `TradeSignal` + `trade_signals.pool_id` (migration 0007) + insert/upsert/rowToSignal plumbing. P3: pricing reads V4 via **StateView** (`getSlot0`/`getLiquidity` by poolId; addresses verified on-chain — also fixed a wrong Base V4 PoolManager constant in `venues.ts`), currencies derived from the swap pair (no PoolKey reverse-lookup), liquidity = virtual quote reserve from L (`method: "v4-virtual-reserves"`); optional `MarketHint` threaded through `getUsdPrice(Result)`/`getLiquidityUsd(Result)`. P4: engine passes `v4MarketHint(signal)` into the buy-decision liquidity/price calls + `handleConfirmed`. **P6 (decoder poolId capture):** live check (`pnpm check-v4 <txHash>`, `packages/pricing/scripts/check-v4.ts`) proved LOCAL's pool reads **$48k liquidity (≈ GMGN's $50k)** — but LOCAL's signals were `venue=balance-delta` with no poolId because native-ETH buys make `strategyA` bail. Fixed: `extractV4PoolId(event)` in `decoder.ts` captures the poolId for any single-V4-swap tx, wired into `buildSignals` so balance-delta signals carry it too (counter resolves to WETH). Validation: `pnpm build` (10 pkgs) and `pnpm test` (17 tasks) green; +3 pricing, +1 paper-engine, +2 decoder tests. Full decoder→engine pipeline not yet re-checked on a fresh live LOCAL trade (pre-fix rows have null poolId).
- **V4 marks + exit-sell depth follow-up: complete** (2026-06-17). Closed the last unjustified V4 deferral. `getV4MarketHintForToken(db, chain, token)` (`packages/store/src/repositories/signals.ts`) recovers `{poolId, counterCurrency}` from the most recent `trade_signals` row touching the token on either leg (poolId is a property of the token's V4 pool, chosen lookup over denormalising onto `positions`). `marks.ts` threads the hint into `getUsdPriceResult` (V4 positions now re-price instead of "no price → skip"); `executeExitSell` threads it into `getLiquidityUsd` (V4 exits use real depth instead of the null-liquidity 500bps penalty). Indexes `trade_signals_token_in_idx`/`trade_signals_token_out_idx` (migration 0008) keep the per-tick marks lookup off a seq-scan. Also corrected two stale docs: fill-level provenance in `paper_fills` is in fact persisted (columns written on copy+skip). **Still deferred (with reason):** live full-pipeline re-check (needs a real post-deploy trade); bounding the TTL-only pricing caches (low risk). Validation: `pnpm build` (10 pkgs) and `pnpm test` (17 tasks) green; +3 store, +1 paper-engine tests.
- **Mempool fast path: complete** (2026-06-18, `feat/mempool-fast-path`, plan in `docs/mempool-fast-path-plan.md`). Cuts mempool-buy → provisional-fill latency from ~5 sequential RPC/DB round trips to ~1 by committing the provisional buy at the leader's *implied* price (cheap quote→USD only) and deferring token discovery + risk vetoes to confirm. Split `decide()` into `liquidityVeto()` + pure `sizeBuy()`; added `recordSkip()` (collapses the ~8 duplicated skip-fill branches); new `handleProvisionalBuy()` (no `findBestMarket`/spot/0x calls, no `takeSnapshot()` on the hot path, fill `priceSource:"leader-implied"`); moved the liquidity + fallback-price-source vetoes into `handleConfirmed`, which reverses via the extracted `reverseProvisional()` (now also backs `handleVoided`). Decisions settled with recommended defaults: no inline cache-only liquidity check, accept the `amountOutMin` qty bias (commented), omit hot-path snapshot. **Follow-up (not replicated at confirm):** `spot-twap-divergence` and 0x `no-executable-route` vetoes for fast-path buys. Validation: `pnpm build` (10 pkgs) and `pnpm test` (17 tasks) green; +7 paper-engine tests (73 total).
- **Polymarket watch + record: complete** (2026-06-19, `feat/polymarket-watch-record`). Watch wallets that trade on **Polymarket** (Polygon, ERC-1155 CTF outcome shares, off-chain CLOB, 0–1 USDC prices) — **record only**: their trades land in the existing candidate feed / `/candidates`, never auto-copied/marked/exited. Source is Polymarket's public **Data API** (`data-api.polymarket.com/trades?user=`), polled per watched polygon wallet by a new decoupled `PolymarketWatcher` (`packages/ingest/src/polymarket/{client,watcher}.ts`) that writes `trade_signals` candidate rows directly via `insertSignal` — it never touches the EVM bus/decoder/pricing/engine. Split `ChainId = EvmChainId | "polygon"` with `EvmChainId = "eth"|"base"`: every AMM-only `Record<…>` (WETH/quote/Chainlink/factories/StateView/stablecoins/Llama-slug/cuBudget/native/rpc maps) re-keyed to `EvmChainId` + `isEvmChain` guards at the decoder/engine/scorer/marks boundaries, so the compiler proves a polygon signal can't reach on-chain pricing and **no fake polygon addresses** were introduced. `RawTxEvent.chain` narrowed to `EvmChainId` (raw tx events are EVM-only). Outcome/USDC token labels upserted so `hydrateToken`/`TokenLink` show "Yes/No" + market question; tx links route to **Polygonscan**; `polygon` added to the wallet chain selector + API enum. Copy is **blocked** for non-EVM candidates (API 400 + Copy button hidden) — the record-only safety boundary. New config `POLYMARKET_POLL_MS`/`POLYMARKET_DATA_API_URL`/`CHAIN_STALE_SEC_POLYGON`; poller surfaces in `/health` via a `chain_state` bump. Also completed an in-tree WIP `cuBudget.ts` extraction so the repo builds. No DB migration (chain columns are plain text). Tests: +10 ingest (client zod parse/reject, 429 retry, buy/sell mapping, getHealth, idempotent re-poll). Validation: `pnpm build` (10 pkgs) and `pnpm test` (17 tasks) green. **Deferred (full copy-trade follow-up):** auto-copy/positions/marks/exits for Polymarket, on-chain Polygon decoding, CLOB depth/slippage, market resolution, a clickable Polymarket market link via a dedicated `eventSlug` column (market URL currently in `reason`).
- **Health & metrics endpoint: complete** (2026-06-17, `feat/health-metrics`, plan in `docs/health-metrics-plan.md`). Operational visibility for the long-running runner so the 72h soak (PLAN §10) is observable. Design: the runner upserts a single-row `runner_health(id, ts, payload jsonb)` heartbeat (migration 0009) every `HEARTBEAT_INTERVAL_MS` (default 10s); `apps/api` reads it — honouring "api reads Postgres, not the runner's bus". A **stale heartbeat is itself the down signal** (a dead/blocked runner can't write). Pure `deriveHealth(input, now, thresholds)` in `packages/core/src/health.ts` rolls DB liveness + heartbeat age + per-chain WS state (`ChainWatcher.getHealth()`: new `connectionState`/`connectFailures` + existing fallback/lastEvent/backfill) + RSS + `chain_state.updatedAt` freshness into `ok|degraded|down` (worst-of). `GET /health` unauthenticated (probe; 200/503 + `{status}`), `GET /metrics` authenticated (full report + raw inputs). New config: `HEARTBEAT_INTERVAL_MS`/`HEARTBEAT_STALE_SEC`/`CHAIN_STALE_SEC_ETH`/`CHAIN_STALE_SEC_BASE`/`RSS_SOFT_LIMIT_MB`. Quiet markets are **not** unhealthy (freshness keys off heartbeat + `chain_state`, never trade volume). Also set `fileParallelism:false` in store vitest config (shared-DB integration files must run serially). Tests: +9 core (`deriveHealth`), +4 store (`runner_health` repo + freshness), +3 ingest (`getHealth`). Validation: `pnpm build` (10 pkgs) and `pnpm test` (17 tasks) green. **Deferred:** runner-local `/livez` for event-loop-lag, Prometheus text format, in-DB RSS-trend history.
